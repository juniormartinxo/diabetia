{
  "name": "diabes-monorepo",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "docker compose --env-file .env -f infra/docker-compose.yml up --build",
    "down": "docker compose --env-file .env -f infra/docker-compose.yml down",
    "prisma:generate": "pnpm -C apps/api prisma generate",
    "migrate:dev": "pnpm -C apps/api prisma migrate dev",
    "bash:api": "docker compose --env-file .env -f infra/docker-compose.yml exec api bash",
    "bash:crawler": "docker compose --env-file .env -f infra/docker-compose.yml exec crawler bash",
    "bash:embedder": "docker compose --env-file .env -f infra/docker-compose.yml exec embedder bash"
  },
  "packageManager": "pnpm@9.0.0"
}
